ECHO PREVENTION & INTERRUPT DETECTION FLOW
==========================================

┌─────────────────────────────────────────────────────────────────────┐
│                         NORMAL CONVERSATION                          │
└─────────────────────────────────────────────────────────────────────┘

User Speaks: "What's the weather?"
        │
        ▼
┌───────────────┐
│  Microphone   │───► Raw audio samples
└───────┬───────┘
        │
        ▼
┌───────────────┐
│  STT (VAD)    │───► Transcription: "What's the weather?"
└───────┬───────┘
        │
        ▼
┌───────────────────┐
│  Echo Filter      │───► Check: Is this echo?
│  - Time window    │     ✓ No recent TTS → NOT ECHO
│  - Text matching  │
└───────┬───────────┘
        │
        ▼ PROCESS
┌───────────────┐
│  Ollama LLM   │───► "The weather is sunny today"
└───────┬───────┘
        │
        ▼
┌───────────────────────┐
│  Echo Filter Storage  │───► Store: "The weather is sunny today"
│  Duration: 2500ms     │     Time window: Now + 2500ms + 500ms
└───────┬───────────────┘
        │
        ▼
┌────────────────────────┐
│  Enable Interrupt Det  │───► audio_source.set_tts_state(True)
└───────┬────────────────┘
        │
        ▼
┌───────────────┐
│  TTS Speaks   │───► "The weather is sunny today" ───► Speaker 🔊
└───────┬───────┘                                             │
        │                                                     │
        ▼                                                     │
  (TTS audio reaches microphone) ◄───────────────────────────┘
        │
        ▼
┌───────────────┐
│  Microphone   │───► Captures TTS echo
└───────┬───────┘
        │
        ▼
┌───────────────┐
│  STT (VAD)    │───► Transcription: "The weather is sunny today"
└───────┬───────┘
        │
        ▼
┌───────────────────┐
│  Echo Filter      │───► Check: Is this echo?
│  ✓ Within window  │     ✓ Text matches stored → ECHO!
│  ✓ 95% similarity │
└───────┬───────────┘
        │
        ▼ DISCARD ✓
    (No loop!)


┌─────────────────────────────────────────────────────────────────────┐
│                         USER INTERRUPTION                            │
└─────────────────────────────────────────────────────────────────────┘

TTS Speaking: "The weather is sunny and..."
        │
        │ (Meanwhile, user speaks loudly)
        │
User Speaks: "WAIT!" (loud)
        │
        ▼
┌───────────────┐
│  Microphone   │───► Raw audio: TTS + User voice (mixed)
└───────┬───────┘
        │
        ├────► To STT (processes mixed audio)
        │
        └────► To Energy Detector
               │
               ▼
        ┌──────────────────┐
        │ Energy Calculator │───► RMS energy
        └──────┬───────────┘
               │
               ▼
        ┌──────────────────────┐
        │ Compare to Baseline  │───► Current: 0.15
        │ Baseline: 0.04       │     Threshold: 3x = 0.12
        └──────┬───────────────┘     Result: 0.15 > 0.12 ✓
               │
               ▼ INTERRUPT DETECTED!
        ┌──────────────────┐
        │ Interrupt        │───► Calls callback in separate thread
        │ Callback Thread  │
        └──────┬───────────┘
               │
               ▼
        ┌──────────────────┐
        │ TTS.stop()       │───► Stops playback immediately
        │ sounddevice.stop()│
        └──────┬───────────┘
               │
               ▼
        ┌──────────────────────┐
        │ audio_source         │───► Disables interrupt detection
        │ .set_tts_state(False)│
        └──────────────────────┘

(Now microphone only captures user voice, no TTS)
        │
        ▼
┌───────────────┐
│  STT (VAD)    │───► Transcription: "WAIT"
└───────┬───────┘
        │
        ▼
┌───────────────────┐
│  Echo Filter      │───► Check: Is this echo?
│  ✗ Not in stored  │     ✗ Doesn't match TTS text → NOT ECHO
│    responses      │
└───────┬───────────┘
        │
        ▼ PROCESS
┌───────────────┐
│  Handle User  │───► Process interruption
│  Input        │
└───────────────┘


┌─────────────────────────────────────────────────────────────────────┐
│                      ENERGY BASELINE TRACKING                        │
└─────────────────────────────────────────────────────────────────────┘

When TTS is NOT playing:
    │
    ▼
For each audio chunk:
    │
    ├─► Calculate RMS energy
    │
    └─► Add to history (last 50 chunks)
        │
        ▼
    Calculate baseline = mean(history)
        │
        Example:
        Chunk energies: [0.03, 0.04, 0.05, 0.04, ...]
        Baseline: 0.04

When TTS IS playing:
    │
    ▼
For each audio chunk:
    │
    ├─► Calculate RMS energy
    │
    └─► Compare to baseline × threshold
        │
        If energy > (baseline × 3.0):
            ▼
        INTERRUPT DETECTED!


┌─────────────────────────────────────────────────────────────────────┐
│                      CONFIGURATION SETTINGS                          │
└─────────────────────────────────────────────────────────────────────┘

Echo Filter:
  • buffer_ms: 500          ─► Time after TTS to filter
  • similarity: 0.80        ─► 80% match threshold
  • max_responses: 3        ─► Keep last 3 responses

Interrupt Detection:
  • threshold: 3.0          ─► Energy must be 3x baseline
  • baseline_window: 50     ─► Use last 50 chunks for baseline

TTS:
  • Interruptible playback  ─► Can stop mid-sentence
  • Duration estimation     ─► For echo filter timing
